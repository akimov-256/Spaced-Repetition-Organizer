import { Button, VerticalBox, HorizontalBox } from "std-widgets.slint";
import { TopicCard } from "components/topic-card.slint";
import { DarkColorPalette, LightColorPalette, ThemeState } from "theme/color-palette.slint";

export component AppWindow inherits Window {
    height: 570px;
    width: 1024px;

    background: ThemeState.isLight ? LightColorPalette.White : DarkColorPalette.Charcoal;

    // Horizontal layout
    HorizontalLayout {
        // Content control space
        VerticalLayout {
            width: 250px;

            spacing: 10px;
            padding: 10px;

            HorizontalLayout {
                Text {
                    vertical-alignment: center;
                    height: 35px;
                
                    text: "Topics";
                    font-size: 24px;
                }
            
                Button {
                    height: 35px;
                    width: 35px;

                    text: "+";
                }
            }

            TopicCard {
                
            }

            TopicCard {
                
            }
        }

        // Content space
        VerticalLayout {
            
        }
    }
}
