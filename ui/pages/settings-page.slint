import { SettingsSection } from "../components/settings-section.slint";
export component SettingsPage inherits Rectangle {
    background: transparent;

    VerticalLayout {
        padding: 10px;

        SettingsSection {
            height: 200px;

            
        }
    }
}