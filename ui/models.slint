export struct TopicData {
    title: string,
    lessons: int,
    due: int,
}

export struct LessonData {
    name: string,
    stage: int,
    previous_review: string,
    next_review: string,
    due: int,
}